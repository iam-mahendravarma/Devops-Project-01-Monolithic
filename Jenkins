pipeline {
    agent any

    environment {
        APP_NAME = "nextjs-app"
        APP_DIR  = "/var/www/html"
        NODE_ENV = "production"
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/iam-mahendravarma/Devops-Project-01-Monolithic.git'
            }
        }

        stage('Install Dependencies') {
            steps {
                dir("${WORKSPACE}/${APP_NAME}") {  // Build inside Jenkins workspace
                    sh 'npm install --save-dev typescript'
                }
            }
        }

        stage('Build App') {
            steps {
                dir("${WORKSPACE}/${APP_NAME}") {
                    sh 'npm run build'
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    // Copy build output to /var/www/html (requires sudo)
                    sh "sudo rsync -a --delete ${WORKSPACE}/${APP_NAME}/ ${APP_DIR}/"
                    // Restart systemd service
                    sh "sudo systemctl restart ${APP_NAME}"
                }
            }
        }
    }

    post {
        success {
            echo '✅ Deployment successful!'
        }
        failure {
            echo '❌ Deployment failed!'
        }
    }
}
